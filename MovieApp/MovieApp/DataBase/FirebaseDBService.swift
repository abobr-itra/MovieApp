import Foundation
import FirebaseAuth
import FirebaseDatabase

class FirebaseDBService {
    
    // MARK: - Public
    
    func saveData<T: Encodable>(dataMode: T) {
        
    }
    
    func getData<T: Decodable>(ofType type: T) {
        
    }
}
